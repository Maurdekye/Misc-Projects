# Default crate layout config file
# All crate prizes are hardcoded; use /givereward with no arguments to get a list of available prizes
# You may also use keys in place of prizes
# leave required_key out to make a free chest that opens without a key
# Refer to 'crate_layouts_sample_file.yml' for examples on how to fill out
